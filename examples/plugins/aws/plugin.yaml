id: aws
version: "0.1.0"
capabilities:
  - custom_step
runtime: docker
allowed_env:
  - AWS_ACCESS_KEY_ID
  - AWS_SECRET_ACCESS_KEY
  - AWS_SESSION_TOKEN
  - AWS_REGION
  - AWS_PROFILE
  - AWS_DEFAULT_REGION
docker:
  image: "amazon/aws-cli:latest"
