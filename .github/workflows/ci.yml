name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Build web UI (embed static)
        run: go tool task webui

      - name: Build WASM plugins
        run: go tool task build-plugin-modules

      - name: Go unit tests with coverage
        run: go test -coverprofile=coverage-unit.txt -covermode=atomic ./... -count=1

      - name: Go integration tests with coverage
        run: go test -tags integration -coverprofile=coverage-int.txt -covermode=atomic -timeout 10m -v ./tests/integration/...

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: ./coverage-unit.txt,./coverage-int.txt
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}

  e2e-k8s:
    name: E2E (k8s proxy + SSO)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      # ubuntu-latest ships a running Docker daemon, so testcontainers spins up
      # real k3s + kubectl + OIDC-provider containers with no extra setup. These
      # tests live behind the k8s_e2e build tag and are excluded from the unit
      # run. TestSSOE2E additionally boots an SSO identity provider, so it shares
      # this one Docker environment rather than a separate job.
      - name: Kubernetes access proxy + SSO e2e (real k3s via testcontainers)
        run: go test -tags k8s_e2e -run 'TestK8sProxyE2E|TestSSOE2E' ./internal/k8sproxy/... ./internal/ssoe2e/... -v -count=1 -timeout 40m

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Run lint + security checks
        run: go tool task lint

  build:
    name: Build (${{ matrix.goos }}/${{ matrix.goarch }})
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/goreleaser/goreleaser-cross:v1.26.2-3
    needs: [test]
    strategy:
      fail-fast: false
      matrix:
        include:
          - {goos: linux,   goarch: amd64, cc: gcc,                     ext: ""}
          - {goos: linux,   goarch: arm64, cc: aarch64-linux-gnu-gcc,   ext: ""}
          - {goos: darwin,  goarch: amd64, cc: o64-clang,               ext: ""}
          - {goos: darwin,  goarch: arm64, cc: oa64-clang,              ext: ""}
          - {goos: windows, goarch: amd64, cc: x86_64-w64-mingw32-gcc, cxx: x86_64-w64-mingw32-g++, ext: ".exe"}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Build web UI (embed static)
        run: go tool task webui

      - name: Build binary
        shell: bash
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "1"
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}
          GOTOOLCHAIN: auto
        run: |
          set -euo pipefail
          VERSION="0.0.0-ci+${GITHUB_SHA:0:12}"
          DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          OUT="honey-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }}"
          go build -trimpath -buildvcs=false \
            -ldflags="-s -w -X main.version=${VERSION} -X main.commit=${GITHUB_SHA} -X main.date=${DATE}" \
            -o "${OUT}" \
            ./cmd/honey
          sha256sum "${OUT}" > "${OUT}.sha256"
          cat "${OUT}.sha256"

      - uses: actions/upload-artifact@v7
        with:
          name: honey-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            honey-${{ matrix.goos }}-${{ matrix.goarch }}*
          if-no-files-found: error
